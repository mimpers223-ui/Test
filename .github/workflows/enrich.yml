name: Enrich Addresses (Nominatim)

# ОТКЛЮЧЁН: enrich работает через Render /api/enrich
# Этот workflow не работает из-за сетевых проблем GitHub Actions с Supabase.

on:
  workflow_dispatch:
    inputs:
      limit:
        description: "Disabled — use Render /api/enrich instead"
        required: false
        default: "500"

jobs:
  enrich:
    runs-on: ubuntu-latest
    if: false  # permanently disabled
    steps:
      - run: echo "Disabled"
